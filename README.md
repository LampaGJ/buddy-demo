# Buddy demo site

This repository only receives built deployments of the Buddy 245D scheduling PoC.
The application source lives in a private repository; the site is published from the
`gh-pages` branch by that repository's deploy workflow.

Live: https://lampagj.github.io/buddy-demo/
